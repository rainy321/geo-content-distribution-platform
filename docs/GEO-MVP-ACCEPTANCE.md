# GEO MVP 验收、交付与部署状态

更新时间：2026-09-05

依据：《GEO 智能内容生成与自动分发平台——极速开发任务书》

## 当前结论

第一版已经达到“本地可运行、Demo 可完整演示、真实模型已验收、知乎、今日头条、搜狐号、百家号和小红书五条真实内容链路均已跑通”的状态。知乎通过公开文章 API 取得唯一匹配链接。今日头条前三次授权暴露了两阶段按钮、默认空单图封面和草稿保存响应语义问题；本地逐项修复后，第四次一次性授权成功完成“预览并发布”和“确认发布”。创作者后台“已发布”页出现精确标题并返回公开 `toutiao.com/item` 链接，本地任务已通过平台证据从 `processing` 对账为 `success`。全过程没有自动重试，原有 4 份自动保存草稿未删除。搜狐号首轮真实提交因平台把旧版“信息来源/包含AI创作内容”改为“创作声明/含有AI生成内容”，同标题内容只形成草稿；在项目所有者重新授权后，本地完成兼容修复，复用唯一草稿，核验标题、正文及 AI 声明后只提交一次。后台先显示“审核中”，随后返回文章 ID `1070776112`、作者 ID `122997873` 及公开链接 `https://www.sohu.com/a/1070776112_122997873`；未登录公网请求为 HTTP 200 且标题精确匹配，任务 `5` 已对账为 `success`。百家号任务 `6/8` 已按六状态无记录的证据结案；第三次授权任务 `9` 首次被平台要求完成人机验证，自动化保持同一任务并在账号本人只完成人机验证后恢复。平台随即自动提交，“已发布”列表与接口均返回精确标题、`status=publish`、文章 ID `1875188881859292891` 和公开链接 `https://baijiahao.baidu.com/s?id=1875188881859292891`；未登录请求为 HTTP 200 且标题匹配，任务 `9` 已通过平台证据从 `need_action` 对账为 `success`，没有创建新任务或重复发布。小红书首轮已成功，创作中心“已发布”筛选与 `/creator/note/user/posted` 接口返回 note ID `6a977888000000001103bb96` 和标准 URL `https://www.xiaohongshu.com/explore/6a977888000000001103bb96`；最新三平台授权未被滥用为重复发布。游客直连仍被平台以“当前笔记暂时无法浏览 / IP 存在风险”拦截，验收记录保留这一可见性限制。发布中心已补齐共享图片上传、任务图片持久化及执行器透传，百家号/小红书不再因业务任务丢失图片而必然进入 `need_action`。真实定时自动执行现在还会把用户确认时的文章、标签、渠道、图片文件名和执行时间固化为授权指纹；到期前任何一项变化都会在选择发布器和访问平台之前转为 `need_action`。

代码已推送至项目所有者的 GitHub 仓库，`main` 已连接 Vercel 并完成公开生产部署：[https://geo-content-distribution-platform.vercel.app](https://geo-content-distribution-platform.vercel.app)。线上管理台、健康检查、Dashboard、项目和素材读取接口均已通过公网与无头浏览器复核。该部署固定为安全 Demo 控制面；真实媒体登录与发布仍只在本地浏览器 Worker 中执行。

Vercel Production 已增加默认模型服务地址、模型名和 Secret 类型 API Key，并重新部署至生产别名。Web 管理台新增“系统设置 → 自定义 AI API”：地址和模型名保存在浏览器本地，密钥只在当前标签页会话内保存；生成和优化请求会优先使用完整的浏览器配置，否则回退服务器默认值。后端只接受公开 HTTPS 服务地址并拒绝明显的本机、私网、保留地址和 URL 内嵌凭据，不跟随模型服务重定向，配置状态接口不返回任何具体值。

生产管理台现已增加单运营者访问门：Vercel 分别保存运营口令和会话签名 Secret，后端签发 HttpOnly、SameSite=Lax、HTTPS-only 会话 Cookie；除健康检查和认证入口外，业务 API 与旧版媒体控制路由均受保护。登录尝试和 AI 请求具有固定窗口限流，未配置 Upstash 时为进程内安全网，配置两个服务端 REST Secret 后自动使用跨实例原子计数。跨域凭据请求不再对任意来源开放。独立 `sau-worker` 入口已复用现有调度器、授权指纹和 PublisherAdapter，可与 Web 进程分离运行。

任务书 P2 内容能力现已完成代码接入：内置/自定义模板、最多 5 主题批量 AI 生成、Excel/CSV 批量导入、素材推荐、1200×628 封面生成及自动配图均可从 GEO Web 使用。发布中心通过统一能力清单展示 10 个渠道，抖音/快手使用图片笔记适配器，Bilibili/视频号/TikTok 使用视频适配器；视频文件名和非敏感账号 ID 已进入任务持久化、授权指纹和执行器路径校验。五个 P2 账号均已由项目所有者完成登录并执行多轮授权验收：Bilibili 任务 `27` 已取得公开 BV 链接，TikTok 任务 `33` 已处理延迟新手引导和版权检查二次确认并取得公开视频链接；视频号新号 `channels-secondary` 重新登录后，任务 `35` 已在官方后台取得精确标题、`2026-09-07 10:09` 发布时间、视频数 `1` 与草稿箱 `0` 的证据并对账为 `success`，平台不提供可验证 Web 公链；抖音任务 `28`、快手任务 `29` 各只点击一次最终按钮且后台仍无同名记录，均已转为 `need_action`。具体结果和继续条件见下方未完成项账本。

## 未完成项账本

本表是必须在最终交付时逐项汇报的持久化清单。暂时缺少授权、登录或本机环境的事项只登记，不阻断其他本地工作；完成后保留验收证据并改为“已完成”。

| 事项 | 当前状态 | 暂未完成原因 | 继续条件 |
| --- | --- | --- | --- |
| 搜狐号真实发布验收 | 已完成 | — | 任务 `5` 复用唯一草稿完成修复后提交；后台文章 ID、已发布记录、标准公开 URL 及公网 HTTP 200/精确标题均已核验 |
| 百家号真实发布验收 | 已完成 | — | 任务 `9` 复用原授权流程；账号本人只完成人机验证，平台随后自动提交。已发布记录、文章 ID、标准公开 URL 及未登录 HTTP 200/精确标题均已核验 |
| 小红书真实发布验收 | 已完成 | — | 任务 `7` 仅提交一次；“已发布”页、posted API、note ID 与标准公开 URL 均已取得。游客直连受平台 IP 风险限制，但不影响创作中心发布证据 |
| GitHub 推送 | 已完成 | — | `origin/main` 已包含内容模板、批量导入/生成、自动配图、P2 Web 适配层及真实验收安全修复 |
| Vercel 生产部署 | 已完成（Demo 控制面） | — | `main` 已连接 Vercel 自动部署；主页与健康接口 HTTP 200，真实浏览器发布仍只在本地 Worker 执行 |
| Docker 镜像实机构建 | 按项目所有者决定跳过 | 本轮改用 Vercel，不再以 Docker 作为交付阻塞项 | 只有后续明确需要自托管镜像时再执行 |
| 应用级认证 | 已完成 | — | 单运营者口令、HttpOnly 会话、登录限速、退出入口和前端访问门均已完成；不扩展为第一期禁止的复杂权限系统 |
| 持久数据库与对象存储 | 暂待项目所有者选择 | Neon/Turso、Vercel Blob 等均需要开通第三方资源并确认套餐/计费；当前 Vercel SQLite 与素材仍为临时数据 | 确认存储供应商与费用后执行数据库迁移、Blob 接入和数据迁移验收 |
| 跨实例共享 AI/登录限流 | 适配器已完成，资源未启用 | Upstash REST 原子计数、服务端配置校验和本地故障降级均已实现；尚未开通第三方 Redis 资源 | 项目所有者选择套餐并提供 Vercel Marketplace/Upstash 资源后，只需配置两个 Secret 并验收 `rate_limit_scope=shared` |
| 独立发布 Worker | 代码与部署自检已完成，托管待选择 | `sau-worker`、`--once`、`--check` 已可运行；Vercel Serverless 不能长驻浏览器且没有持久 Cookie/素材 | 选择受控长驻主机并挂载与 Web 共用的持久数据库、Cookie 和素材目录；先运行 `sau-worker --check` |
| 千问超时修复后的真实生成复验 | 已完成 | — | 项目所有者授权一次 600 字、不保存不发布的连接验收；通过生产运营会话调用服务端 `qwen3.8-max`，18.9 秒成功返回标题、摘要、1166 字正文、6 个标签和 3 个 FAQ。未保存文章、未触发任何发布 |
| 抖音 P2 真实验收 | 未落单，待人工处置 | 最新任务 `28` 已确认 AI 声明并只点击一次发布；`2026-09-07` 再次只读遍历全部/已发布/审核中/未通过，登录有效但仍无精确标题，任务保持 `need_action` | 只读复核已完成且仍无记录；再次真实提交必须取得项目所有者新的逐次授权 |
| 快手 P2 真实验收 | 未落单，待人工处置 | 最新任务 `29` 已确认 AI 声明并只点击一次发布；`2026-09-07` 再次只读遍历全部作品/已发布/待发布/未通过，登录有效但仍无精确标题，任务保持 `need_action` | 只读复核已完成且仍无记录；再次真实提交必须取得项目所有者新的逐次授权 |
| Bilibili P2 真实验收 | 已完成 | 任务 `27` 在官方冷却后上传成功；内容管理页精确标题与公开 BV 链接均已核验 | 公开链接 `https://www.bilibili.com/video/BV1xJt66fEyq/`，不再重复发布 |
| 视频号 P2 真实验收 | 已完成 | 新号 `channels-secondary` 重新登录后确认此前是假阳性登录判断；修复上传诊断和可选原创声明后，任务 `35` 已在官方视频管理页出现精确标题与 `2026-09-07 10:09`，视频数 `1`、草稿箱 `0`，本地对账为 `success` | 平台未提供可验证 Web 公链，`result_url` 保持为空；不再重复发布 |
| TikTok P2 真实验收 | 已完成 | 任务 `33` 精确处理版权检查未完成弹窗的一次“立即发布”确认，随后内容管理页按精确标题取得公开链接并对账为 `success` | 公开链接 `https://www.tiktok.com/@redemption3731/video/7681719912934477072`，不再重复发布 |
| 真实定时任务到点自动执行 | 已完成 | — | 默认只创建普通计划；用户针对文章、时间、渠道及图片再次确认后才持久化自动执行授权和内容指纹。到期时指纹不一致会在访问平台前阻断；执行仍受真实发布总开关保护，失败或状态不明不自动重试 |

## P0 验收矩阵

| 任务书能力 | 状态 | 当前实现与证据 |
| --- | --- | --- |
| 项目创建 | 已完成 | 品牌项目 CRUD 页面与 `/api/projects` |
| AI 文章生成 | 已完成 | OpenAI-compatible 生成服务与 `/api/articles/generate`；已使用 qwen3.8-max 完成一次真实调用 |
| 文章编辑 | 已完成 | 内容库编辑器与 `/api/articles/:id` |
| GEO Score | 已完成 | 五维规则评分、建议与 `/api/geo/score` |
| 至少一个真实 Publisher | 已完成 | 知乎与今日头条均完成人工登录、Cookie 校验、真实 API 门控、编辑器填写、AI 创作声明、精确发布及公开链接核验 |
| 一键发布 | 已完成 | 多平台独立建任务；图片先进入素材库并随任务保存，百家号/小红书缺图时阻止创建真实任务；Demo 自动执行，五个真实渠道均需二次确认和有效登录账号 |
| 发布状态 | 已完成 | `queued → processing → success/failed/need_action`，另有 `scheduled`；发布中心以串行 5 秒轮询刷新，慢请求不会叠加，离开页面后停止 |
| 发布历史 | 已完成 | 发布中心筛选、分页、重试、Demo 明示；手动重试会撤销旧的定时自动授权，真实执行仍需再次确认 |
| Dashboard | 已完成 | 今日指标、七日趋势、媒体连接与最近发布，数据来自 SQLite |
| Demo Mode | 已完成 | `DemoPublisher` 不访问真实平台；空库自动准备明确标注的示例数据 |

## P1 验收矩阵

| 任务书能力 | 状态 | 说明 |
| --- | --- | --- |
| 2–4 个真实媒体平台 | 已完成 | 知乎、今日头条、搜狐号、百家号和小红书五条真实账号内容链路均已取得标准公开链接并完成本地任务对账 |
| AI 一键 GEO 优化 | 已完成 | 优化预览、前后分数与保存流程；真实模型验收从 85 分提升到 100 分 |
| 定时发布 | 已完成 | APScheduler 原子领取；Demo 到期自动执行。真实任务默认到期后保持排队，只有创建计划时完成第二次明确确认、授权内容指纹到期仍匹配且服务端真实发布总开关仍开启，才会自动尝试一次；失败或状态不明不自动重试 |
| 多平台同时发布 | 已完成 | 每个平台一条独立任务，单平台失败不阻塞其他平台 |
| 简单统计图 | 已完成 | Dashboard 七日生成、任务、成功三序列趋势 |

## P2 验收矩阵

| 任务书能力 | 状态 | 说明 |
| --- | --- | --- |
| 小红书 | 已完成并真实验收 | Web 图文任务、图片素材、AI 声明和公开链接对账均已跑通 |
| 内容模板 | 已完成 | 4 个内置模板和自定义模板 CRUD；单篇/批量生成均可引用 |
| 批量文章 | 已完成 | 单次最多 5 主题，逐篇保存草稿；模型失败后停止余下调用并明确标记跳过 |
| Excel 导入 | 已完成 | 标准模板下载，支持 `.xlsx` 与 UTF-8 `.csv`，单次最多 200 行并返回行级错误 |
| 图片自动生成 | 已完成 | 本地生成 1200×628 中性 GEO PNG，并登记到统一素材库 |
| 自动配图 | 已完成 | 按项目、标题和标签推荐现有图片，无匹配时受控生成封面 |
| 抖音/快手/Bilibili/视频号/TikTok | 代码完成，实号验收部分完成 | Bilibili、TikTok 已取得公开链接并完成本地对账；视频号新号任务 `35` 已凭官方后台证据对账成功；抖音、快手最新任务无后台记录并保持 `need_action` |

## Demo 验收路径

PowerShell：

```powershell
$env:DEMO_MODE="true"
$env:SEED_DEMO_DATA="true"
uv run --extra web python sau_backend.py
```

另一个终端：

```powershell
cd sau_frontend
npm run dev
```

访问 `http://127.0.0.1:5173` 后可依次验证：

1. Dashboard 首屏非空，最近发布明确标注 `DEMO / 演示`。
2. “品牌项目”显示“XX科技（示例数据）”。
3. “内容库”显示 6 篇带“示例数据”标签的文章。
4. “AI 内容创作”可在配置模型后生成文章；未配置时返回明确的配置错误。
5. “GEO 优化”可评分并调用 AI 优化。
6. “发布中心”可多选平台、即时或定时创建任务；Demo 任务不访问真实平台。
7. “媒体账号”首次加载只读本地状态，不自动访问平台。

## 已执行验证

```text
后端：332 tests passed
前端：Vite production build passed（1714 modules）
Vercel：提交 `ea02bc6` Production Ready；主页和 `/backend/api/health` 均为 HTTP 200，健康接口确认 Demo/临时存储边界，最近一小时无运行时错误
公网浏览器：Dashboard 中文界面与示例数据正常；控制台错误、页面异常、失败请求和 4xx/5xx 响应均为 0
浏览器：隔离 Demo 首次启动、Dashboard、发布中心导航、关键 UI、错误层、控制台和失败请求检查均通过；复验端口已关闭
真实模型：qwen3.8-max 生成 688 字 / GEO 85，优化 952 字 / GEO 100
安全边界：真实发布确认弹窗取消后 /execute-real 请求数为 0
知乎实号首轮：登录与 Cookie 校验成功；真实 API 返回 200/processing；两次公开文章查询均为 0，编辑器重新打开为空
知乎授权重试：精确“发布”触发页面导航，公开文章 API 返回唯一匹配文章与链接 `https://zhuanlan.zhihu.com/p/2078070222595035345`
知乎根因与修复：模糊“发布”按钮会误点“发布设置”；现已精确匹配、识别发布后导航、要求可观察提交信号，并支持使用平台证据对账本地状态
知乎防重复：正式适配器在发布前精确查询同标题公开文章，发布后最多轮询 3 次公开结果；查询失败时阻止新发布，不会盲目重复点击
今日头条第二适配器：复用原 `TouTiaoArticle` 与 Cookie 登录；真实执行沿用总开关、非 Demo、二次确认三重门控
今日头条结果边界：仅公开文章链接可标记 success；已提交、等待审核或超时未知保持 processing，不自动重试以避免重复发文
今日头条登录检测：修复 SPA 跳转依赖 `wait_for_url/load` 导致的误超时，改为轮询创作者后台 URL；2026-09-01 人工登录成功进入创作者后台，Cookie 即时校验及独立复核均有效，媒体账号服务返回 `connected`
今日头条话题预演：首次 dry-run 发现“功能测试”会模糊命中“心脏功能测试”、“今日头条”会命中“今日头条微头条”；真实发布被阻止。现已改为规范化后的完整话题名精确匹配，危险近似候选回归用例通过；第二次 dry-run 未选入任何错误话题
今日头条首次授权执行：任务 `1` 在提交前以 `failed` 结束，错误为精确旧版“发布”按钮数量 0；未点击“预览并发布”、未产生提交请求、未取得公开链接，一次性尝试标记已落盘且没有自动重试
今日头条第二次授权执行：任务 `2` 点击唯一“预览并发布”后未出现最终确认控件，随即以 `failed` 停止；当时尚未识别出页面其实仍停留在编辑器，没有最终确认、没有公开链接、没有自动重试
今日头条第三次授权执行：任务 `3` 再次点击唯一“预览并发布”，精确 ARIA 与 `button.publish-btn-last` 文本兜底在 15 秒内仍未找到最终确认控件，随即以 `failed` 停止；没有最终确认、没有公开链接、没有自动重试
今日头条前三次后的平台复核：公网精确标题搜索为空；创作者“已发布/审核中/审核未通过/仅我可见”均无同名内容；“草稿箱”有 4 份同名自动保存稿，均只显示“编辑/删除”；未擅自删除用户草稿
今日头条无最终点击诊断：复用最新草稿并只点击第一阶段按钮；接口 `/mp/agw/article/publish` 返回 `code=0` 和“保存成功”，页面仍停留在编辑器，按钮持续可见。截图确认默认“单图”已选中但封面框为空
今日头条根因验证：把同一草稿显式切换到“无封面”后再次只点击第一阶段按钮，“预览并发布”随即消失，页面出现手机预览遮罩和唯一“确认发布”按钮；诊断脚本没有实现或执行最终点击
今日头条修复：无封面文章不再跳过封面设置，而是精确选择并核验“无封面”；无法选中时在发布前失败。单图上传也复用同一精确模式选择器；新增 2 项封面回归测试
今日头条响应边界：URL 含 `publish`、HTTP 200 或单独的 `code=0` 均不再被当作已接受发布；实测“保存成功”只算草稿写入。只有明确的 `submitted/auditing/published` 状态或“提交成功/审核中”等语义才算已接受；新增 2 项响应回归测试，完整测试为 207 项通过
今日头条第四次授权执行：任务 `4` 使用修复后的“无封面”模式，成功点击唯一“预览并发布”和“确认发布”；平台先返回已接受提交，本地保持 `processing` 且未自动重试
今日头条最终平台复核：创作者后台“已发布”页出现精确标题并提供公开链接 `https://www.toutiao.com/item/7680497457658135090/`；“审核中/审核未通过/仅我可见”无同名内容，公开 URL 独立请求返回 HTTP 200。任务 `4` 已通过平台证据对账为 `success`，文章状态同步为 `published`，真实自动发布验收通过
搜狐号第三适配器：基于现有无封面图文上传器接入统一真实任务工厂和 API 门控；自动声明“包含AI创作内容”，只允许唯一精确发布按钮，并监测平台写入响应
搜狐号结果边界：仅 `www.sohu.com/a/...` 公开文章链接可标记 `success`；平台明确接收、写入状态不明或超时均保持 `processing` 且不自动重试；登录、滑块和风控映射为 `need_action`
搜狐号本地验证：新增发布观察、适配器、账号工厂与真实 API 门控回归用例；平台非标准发布控件以唯一精确 DOM 结构识别，禁止模糊点击
搜狐号账号登录：账号 `sohu-main` 已完成人工登录，Cookie 即时校验与独立浏览器复核均有效，本地媒体账号记录为 connected
搜狐号授权 dry-run：测试稿标题精确匹配，正文 858 字已填入，无封面；脚本没有调用或点击任何发布、确认发布或最终确认控件。平台页面显示“未实名暂无法发布文章”，发布/定时发布/存草稿/预览均禁用，因而 AI 创作来源选项也未能完成选择
搜狐号实名阻塞保护：现已在填写标题和正文之前识别平台精确禁发文案并返回 `need_action`；普通实名认证提示不会误判为硬阻塞。新增 2 项回归测试，完整后端测试增至 222 项
搜狐号实名后复验：页面不再出现“未实名暂无法发布文章”，测试稿标题与正文再次成功写入，无封面；底部唯一 `li.publish-report-btn.active.positive-button` 发布控件可见且启用。脚本只读检查 DOM，没有点击发布或任何确认按钮。当前账号页面没有暴露“包含AI创作内容”选项，因此没有伪造已声明结论
百家号统一适配器：已接入真实账号类型、工厂与 API 三重门控；当前可由操作者选择展示封面或启用受控自动配图，最终缺图仍会在登录和发布前阻断；只有公开 `baijiahao.baidu.com/s?id=...` 链接才算成功
小红书统一适配器：图文笔记缺少用户提供的图片时在登录和发布前返回 `need_action`；统一流程只精确点击一次发布按钮，验证码/安全验证交还人工，内部成功页或最终状态不明保持 `processing` 且不自动重试；只有公开笔记链接才算成功
百家号与小红书账号登录：账号 `baijiahao-main`、`xiaohongshu-main` 均已完成人工登录，即时校验及全新无头浏览器独立 Cookie 复核均有效，本地媒体账号记录为 connected
百家号与小红书只读入口检查：百家号图文编辑器的标题、正文、封面上传、AI 声明和发布控件均存在；小红书进入图文发布页并找到图片上传控件，没有回到登录框。检查过程未填写内容、未上传图片、未保存草稿、未点击发布
中性测试封面：经项目所有者授权，使用内置图像生成工具生成无品牌、无平台 Logo、无文字、无数字和无效果承诺的中性 GEO 分发示意图，保存为 `media/geo-test-cover.png`，仅用于百家号和小红书链路测试
百家号授权 dry-run：测试稿标题、纯文本正文和中性封面填写成功，“采用AI生成内容”已勾选；页面级预览保护禁用了发布动作，截图与日志均确认脚本没有点击发布或任何确认按钮
小红书首轮授权 dry-run：图片、标题、正文和“笔记含AI合成内容”写入成功，但截图发现 Markdown 标题标记被平台解释为话题语法，连续提示“话题内不允许包含特殊符号”，因此未将首轮误报为通过；脚本没有点击发布
小红书修复与复验：百家号/小红书适配层现将 Markdown 保守转换为保留段落的纯文本，小红书模式额外去除 `#` 话题触发符；dry-run 新增顶部警示、精确发布控件锁定和页面错误断言。一次纠正复验后特殊符号错误消失，正文计数 776/1000，AI 声明正确，脚本跳过发布并关闭浏览器；失败后未继续自动重跑
三平台授权真实提交：项目所有者明确授权后，任务 `5/6/7` 分别对搜狐号、百家号、小红书只执行一次真实发布流程；三个账号 Cookie 均有效，测试稿标题与正文一致，百家号/小红书使用同一中性封面，百家号勾选“采用AI生成内容”，小红书勾选“笔记含AI合成内容”。任何平台均未自动重试
搜狐号首轮真实结果：任务 `5` 观察到文章写入请求但没有公开链接；后续只读复核确认同标题内容实际为“草稿”，根因是平台声明文案已从“信息来源/包含AI创作内容”迁移为“创作声明/含有AI生成内容”
搜狐号重新授权结果：兼容修复保留旧参数别名并映射到当前声明；复用唯一草稿，在标题、正文和“含有AI生成内容”均核验通过后只提交一次。内容管理精确标题于 `2026-09-02 10:42` 先显示“审核中”，随后出现阅读/评论指标及公开链接 `https://www.sohu.com/a/1070776112_122997873`；接口记录 ID `1070776112`、作者 ID `122997873`、状态 `4` 一致，公网未登录请求 HTTP 200 且标题匹配。任务 `5` 已对账为 `success`，没有自动重试
百家号首轮真实结果：任务 `6` 观察到文章写入请求，但“全部/已发布/待发布/未通过/已撤回/草稿”六个状态页均无同标题记录；平台证据已写回并结案为 `failed`
百家号重新授权结果：独立任务 `8` 完成 Cookie、标题、正文、中性封面与“采用AI生成内容”校验后只执行一次发布及唯一确认；平台仍只暴露写入请求，提交后六状态立即复核无精确标题，没有自动重试
百家号延迟复查与结案：只读复查一度出现 `passMod` 安全验证遮罩；可见浏览器解除验证并刷新 Cookie 后，再次完整检查“全部/已发布/待发布/未通过/已撤回/草稿”，精确标题和列表接口记录仍均为空。任务 `8` 已从 `processing` 结案为 `failed`，本轮授权不再提交
百家号失败响应边界：发布观察器现在识别非 2xx、非零错误码、`success/accepted=false`、明确失败状态与拒绝文案；普通拒绝直接记 `failed`，登录/验证码/风控类拒绝记 `need_action`，不再把平台已明确拒绝的写请求长期留在 `processing`
百家号第三次授权结果：执行前再次只读确认六个状态均无精确标题；独立任务 `9` 完成 Cookie、标题、正文、中性封面及“采用AI生成内容”校验并点击发布。平台返回“您所在网络环境异常，请完成验证”；账号本人只完成人机验证后，平台自动继续提交，自动化未再次点击不存在的发布按钮，也没有创建第四个任务或重复发文
百家号人工验证后对账：创作中心“已发布”列表与 `/pcui/article/lists` 接口均返回精确标题、`status=publish`、文章 ID `1875188881859292891`、更新时间 `2026-09-02 11:25:04` 和标准公开 URL `https://baijiahao.baidu.com/s?id=1875188881859292891`；待发布、未通过、已撤回、草稿均无同名记录。未登录公开请求 HTTP 200 且标题匹配，任务 `9` 已通过平台证据从 `need_action` 对账为 `success`
平台证据状态恢复：`reconcile_publish_job_success` 现在允许人机验证前处于 `need_action` 的任务在取得已发布记录与公开链接后直接对账为 `success`；仍不允许无链接或普通排队任务越级成功，并新增回归测试
小红书真实结果：任务 `7` 跳转发布成功页；创作中心“已发布”筛选出现精确标题、封面和 `2026-09-02 09:14` 发布时间，posted API 返回 note ID `6a977888000000001103bb96`。标准 URL `https://www.xiaohongshu.com/explore/6a977888000000001103bb96` 已写回任务并通过平台证据对账为 `success`；游客直连因“当前笔记暂时无法浏览 / IP 存在风险”受限，没有重发
小红书发布后自动对账：统一适配器在唯一一次发布跳转成功页后，复用同一浏览器会话只读进入“笔记管理→已发布”，按精确标题解析 posted API 的 24 位 note ID 并生成标准公开 URL；对账过程不再点击发布。已覆盖内部成功页、嵌套响应 ID、同会话已发布筛选和适配器标题透传测试
图片任务本地闭环：发布中心复用 `/uploadSave` 上传一张共享 JPG/PNG；API 只接受素材目录内实际存在的安全文件名，任务以 JSON 保存最多 9 张图，执行器发布前再解析为素材目录内绝对路径。百家号和小红书真实任务缺图时在创建阶段直接返回明确错误
图片链路回归：隔离数据库与素材目录内完成“上传图片 → 返回唯一文件名 → 创建百家号任务 → Demo 执行器解析图片 → 查询任务图片”的端到端测试；旧 `publish_jobs` 表会幂等补齐 `images` 字段，既有任务默认 `[]` 且数据保留
分发中心浏览器复核：最新桌面回归确认 10 个渠道均渲染，选择 Bilibili 后视频资产位切换为必需状态；模板管理、批量生成入口、Excel 导入、P2 账号卡片均可见，页面非空且无错误覆盖层、控制台错误或页面异常。此前 390px 响应式复验仍通过
定时自动执行安全链路：默认关闭；创建时弹窗明确列出文章、到点时间和渠道数，关闭弹窗不创建任务，“只创建普通计划”提交 `auto_execute=false`，明确确认才提交 `true`。服务端严格校验布尔值、定时字段与真实发布总开关；调度器只执行持久化授权任务，总开关/发布器缺失时不访问平台并转为 `need_action`，不会自动重试。浏览器拦截本地 API 验证两条分支，无控制台错误、失败请求或横向溢出
定时授权内容绑定：真实自动计划在同一数据库事务内对文章 ID、标题、正文、标签、平台、图片文件名和执行时间生成 SHA-256 指纹；API 只返回是否已绑定，不暴露原始指纹。到期执行在构造发布内容、选择发布器和访问平台前重新计算；文章内容变化的回归用例确认任务转为 `need_action`、真实尝试计数为 0、发布器工厂调用次数为 0
发布状态串行轮询：发布中心从固定 `setInterval` 改为每次只在上一轮任务查询结束后再等待 5 秒；慢请求不会叠加或并发覆盖状态，页面卸载会清理等待中的计时器，正在结束的请求也不会复活下一轮。新增源码生命周期交付测试
手动重试授权撤销：`failed/need_action` 任务重新排队时会同时清空旧授权指纹并关闭 `auto_execute`；任务不会继续显示“已授权”，真实任务必须再次点击“确认真实发布”，从数据层落实“失败或状态不明不自动重试”
素材接口配置隔离：上传、列表、预览、下载和删除均统一使用运行时 `DATABASE_PATH` / `MEDIA_ROOT`；补齐前端既有下载按钮对应的后端路由，并拒绝不安全文件名。隔离数据库与临时素材目录内已验证完整生命周期，不读取或删除默认目录数据
账号登录存储隔离：Web 登录线程向 10 个平台登录函数显式传递运行时 `DATABASE_PATH` / `COOKIES_DIRECTORY`；账号总览、Cookie 检测、即时真实发布和定时发布器工厂使用同一 Cookie 根目录。TikTok 已加入类型 10 的存储与调度回归
登录 SSE 生命周期：`/login` 在启动线程前校验平台与账号名，以“平台+账号”作为会话键并拒绝同一会话重复启动；SSE 在 `200/500` 终态后结束，在正常结束或客户端断开时幂等清理全局队列，空闲期间发送心跳以便断开可被感知
本轮完整验证：361 项后端测试、Vite 1714 modules 生产构建、12 项隔离 Demo 浏览器验收及 Python 编译检查均通过
P2 合规测试视频：本地生成 10 秒、720×1280、H.264、无音频的中性几何动画 `videoFile/geo-p2-neutral-test.mp4`，无人物、音乐、平台 Logo、品牌和效果承诺；已登记为本地素材 `1`
P2 五平台真实验收：五个账号均由项目所有者登录并明确授权同一测试稿。Bilibili 任务 `27` 已在官方冷却后上传成功，内容管理精确标题和公开链接 `https://www.bilibili.com/video/BV1xJt66fEyq/` 均已核验；TikTok 任务 `33` 已精确处理版权检查二次确认，内容管理精确标题和公开链接 `https://www.tiktok.com/@redemption3731/video/7681719912934477072` 均已核验；视频号新号 `channels-secondary` 重新登录并修复假阳性登录判断、上传诊断及可选原创声明后，任务 `35` 已在官方后台核验为视频 `1`、草稿 `0`、发布时间 `2026-09-07 10:09`，本地对账为 `success`；抖音任务 `28`、快手任务 `29` 各只点击一次最终发布且 AI 声明成功，但后台状态页无同名记录，均为 `need_action`
P2 防重复与 AI 声明保护：抖音、快手、视频号最终按钮实行单次点击；结果不明时抛错且不自动重试。抖音和快手图文复用完整 AI 声明定位器，声明无法确认时会在最终发布按钮前停止。TikTok 等待异步编辑器、关闭 React Joyride 引导、按文本精确点击一次 `Post/发布`，且仅对“版权检查未完成”这一已知弹窗点击一次“立即发布”；未知弹窗仍停止并保存诊断截图。快手歧义等待上限约 5 分钟
交付安全：Docker 构建上下文已排除 Cookie、SQLite、`.tmp` 和根目录 `.env`；前端 lockfile 已纳入版本控制，Python 包声明已包含 `services/` 与 `db/`
Python 发布物：最新源码已重新执行 `uv build`；wheel SHA-256 为 `b99fe74ed148bec8fcfb43c57a17652d0b2499b93225bb4be6b0a3023ab9745`，sdist SHA-256 为 `eba20f7562cc9df96b40270aab4e9e5d209650579783ddc2fa2586281c07c0e3`。逐项扫描确认敏感模式、Cookie/数据库/`.env` 等禁止文件、必需模块缺失均为 0
Python 安装烟雾：wheel 已用 `--no-deps` 安装到隔离目录，核心模块确认从该目录加载；CLI 在声明依赖已安装的环境中执行 `sau --help` 成功
部署烟雾：新增 `/api/health` 与容器 HEALTHCHECK；隔离 Demo 数据库实测 health、Dashboard、SPA 根路径和 `/project-management` 深链接均为 HTTP 200，未知 API 保持 JSON 404。Vercel 生产环境通过稳定 `/backend` 前缀连接前后端，公网健康、看板、项目和素材读取接口均返回 JSON 200
监听安全：源码直接启动默认仅监听 `127.0.0.1:5409`，真实进程 health 返回 200；容器通过环境变量显式监听 `0.0.0.0`，再由 Compose 限制映射到宿主机回环地址
Windows 演示入口：`start-win.bat` 从自身目录启动、优先使用项目虚拟环境，并仅在未显式配置时启用 Demo 数据与关闭真实发布；前端只绑定回环地址
产品品牌壳：清除上游 SAU/Vite 浏览器元数据残留；页面语言为 `zh-CN`，标签标题为“GEO 智能内容分发平台”，深青绿 `G` favicon 与侧栏标识一致，并有源码回归断言
交付文档路由：README 与主安装文档的克隆命令均指向项目所有者仓库 `rainy321/geo-content-distribution-platform`，上游 OmniPost/social-auto-upload 仅保留为来源致谢；前端安装改用 lockfile 驱动的 `npm ci`，并有防止主安装文档退回上游克隆地址的回归断言
安装配置安全：主安装文档不再要求用旧 `conf.example.py` 覆盖环境变量驱动的 `conf.py`；示例配置与 `.env.example` 均使用 `LOCAL_CHROME_HEADLESS=true`、`DEBUG_MODE=false` 等安全默认值，不含密钥。GEO Web 安装明确包含 `.[web]`、Playwright Chromium 和前端 lockfile 构建步骤
真实执行文档与提示：README 的五平台真实执行入口和定时授权指纹语义已与实现对齐；真实任务误用 Demo 执行入口时返回明确的入口错误，不再误报“真实平台尚未开放”
容器品牌资源：Docker 构建阶段显式把 `geo-favicon.svg` 复制到最终镜像；`/favicon.ico` 兼容入口也优先返回同一 GEO SVG，避免容器或旧浏览器回退到 Vite 图标
部署浏览器：Vercel 公开生产站真实 Vite 构建产物加载成功，页面非空、示例看板正常，无错误覆盖层、控制台错误、页面异常、失败请求或 4xx/5xx 响应；Dashboard 截图视觉核对通过。首页、health、Dashboard、项目、素材和 favicon 均为 HTTP 200，标题、语言和 SVG Content-Type 均符合预期
自定义 AI 配置浏览器验收：从侧栏进入系统设置，填写测试地址、测试模型和测试 Key 后状态即时切换；桌面和 390px 移动端横向溢出均为 0，保存没有调用模型服务，测试 Key 仅出现在 `sessionStorage` 而未进入 `localStorage`，控制台错误、页面异常、失败请求和 4xx/5xx 响应均为 0
运营访问门浏览器验收：未登录业务 API 返回 401，错误口令保持在访问门，正确口令后 Dashboard 与系统设置可用，退出后业务 API 再次返回 401；会话 Cookie 为 HttpOnly，口令未进入 localStorage/sessionStorage，桌面与 390px 横向溢出均为 0，页面异常与失败请求为 0

千问生产超时修复：Vercel 日志确认 `qwen3.8-max` 首次线上生成在约 121 秒后返回 502；阿里云官方资料确认该混合思考模型默认开启思考。阿里云托管的 Qwen 3.8 Chat Completions 现显式使用非思考模式并限制最多 4096 输出 Token，其他自定义 OpenAI-compatible 服务不注入阿里云专用参数；外部调用补充了不含 Prompt、密钥的主机、模型、耗时和错误类型日志。

千问复验安全边界与结果：生产端 `AI_BASE_URL`、`AI_API_KEY`、`AI_MODEL` 均保持为 Vercel Secret；CLI 的 `env pull` 与 `env run` 均按平台规则拒绝导出三个明文值。验证过程没有把 `[SENSITIVE]` 占位符产生的 401 记录成真实服务失败。重置 `APP_ACCESS_PASSWORD`、重部署并建立临时认证会话后，生产端 `qwen3.8-max` 在 18.9 秒成功返回标题 25 字、摘要 68 字、正文 1166 字、6 个标签和 3 个 FAQ；接口只返回内存结果，未保存文章、未创建发布任务。

生产 AI 验收入口：新增 `scripts/verify_production_ai.py`，只在人工通过运营访问门后调用现有生成接口，控制台仅输出耗时和字段长度，不打印正文或 Secret；登录页导航时的瞬时请求中断会被视为可恢复轮询，不再误报最终失败。本轮已用同一生产接口独立完成真实验收。

P2 运维脚本入口：`open_p2_logins.py`、`preflight_p2_video_publish.py`、`run_authorized_p2_publish.py`、`verify_p2_creator_lists.py` 现在会自行加入仓库根目录，直接按文件运行不再依赖人工设置 `PYTHONPATH`；四个入口的 `--help` 冒烟检查均通过。

Vercel CLI 部署上下文：新增 `.vercelignore`，显式排除本地 `.venv`、浏览器依赖、Cookie、SQLite、素材、临时验证产物和本地环境文件，防止本地完整 Worker 环境被误打包进轻量云端控制面。
请求安全：AI 请求可按客户端配置每分钟上限，达到阈值返回 429/Retry-After；自定义模型地址禁止 HTTP、本机/私网/保留地址、内嵌凭据及 3xx 重定向；认证端点禁止缓存，业务响应带 nosniff、DENY frame、same-origin referrer 与权限策略安全头
共享限流：新增可选 Upstash REST 适配器，以 Lua 在服务端原子完成计数、上限判断和窗口过期；客户端标识只以 SHA-256 摘要作为 Redis Key。Upstash 不可用时回退到同步预热的本地计数器；健康与认证状态接口只公开 `instance/shared` 范围，不暴露地址或 Token
独立 Worker：新增 `sau-worker`、`sau-worker --once` 和不访问平台的 `sau-worker --check`，继续复用原子领取、真实发布总开关、授权指纹和 PublisherAdapter；Demo/真实执行隔离回归通过，Web 可用 `RUN_PUBLISH_SCHEDULER=false` 关闭内置调度器
容器编排：仍保留默认绑定 `127.0.0.1:5409`、关闭真实发布并持久化 SQLite/Cookie/媒体目录的 `compose.yaml`；项目所有者本轮明确选择 Vercel，Docker 实机构建不再作为本次交付阻塞项
工作区：为修复真实预演发现的话题污染风险，对 `uploader/toutiao_uploader/main.py` 做了单点精确匹配修复并新增回归测试；未发现硬编码 API Key；Cookie、`.tmp/`、本地数据库均被 Git 忽略
```

## 必须由项目所有者参与的外部步骤

以下事项不能通过本地 Mock 代替，也不应由自动化擅自执行：

1. 已完成知乎账号人工登录、本地 Cookie 校验与一篇授权测试稿真实发布；后续如 Cookie 失效，需要项目所有者重新登录。
2. 已完成今日头条账号人工登录、Cookie 校验及一次修复后的授权真实发布；平台“已发布”页和公开链接均已核验。后续再次真实发布仍需项目所有者明确授权；系统不会自动重试或自动删除草稿。
3. 真实链路的最终成功依据是平台公开文章链接，而不是自动化脚本是否完成点击；本次知乎链接为 `https://zhuanlan.zhihu.com/p/2078070222595035345`，今日头条链接为 `https://www.toutiao.com/item/7680497457658135090/`。
4. 搜狐号任务 `5` 在项目所有者重新授权后，已复用首轮唯一草稿完成一次修复后的真实提交；公开链接 `https://www.sohu.com/a/1070776112_122997873`、后台记录及公网 HTTP 200/精确标题均已核验，任务已对账为 `success`。
5. 百家号任务 `6/8` 已按六状态无记录的证据结案为 `failed`；第三次授权任务 `9` 在账号本人只完成人机验证后由平台自动继续提交，已发布记录、文章 ID `1875188881859292891`、标准公开 URL 和未登录 HTTP 200/精确标题均已核验，本地任务已对账为 `success`，没有创建新任务或重复发布。小红书任务 `7` 已有 note ID、标准公开 URL 并对账为 `success`，没有重复发布。
6. Vercel 已提供公开 HTTPS 生产入口、服务器端 AI Secret、单运营者访问门和应用内 AI 限流；业务 API 不再允许匿名访问，跨域凭据请求只接受显式白名单。仍待项目所有者选择并开通持久数据库、对象存储和跨实例共享限流资源，再把 `sau-worker` 部署到受控长驻主机。
7. 当前 `origin` 指向项目所有者仓库 `rainy321/geo-content-distribution-platform`，`main` 已推送并连接 Vercel；`upstream` 保留 OmniPost 的 `rehatRobot/omnipost`，后续提交只推送到 `origin`。
8. Python wheel 与 sdist 已在本机构建、审计并完成隔离安装烟雾；Dockerfile 和 Compose 仍保留，但项目所有者本轮明确改用 Vercel，未执行 Docker 实机构建。
9. P2 五渠道均已完成账号登录、素材准备和多轮授权验收。Bilibili 任务 `27`、TikTok 任务 `33` 已取得公开链接并对账成功；视频号新号任务 `35` 已取得精确标题、发布时间、发布列表视频 `1` 与草稿箱 `0` 的官方后台证据并对账成功，平台不提供 Web 公链；抖音任务 `28`、快手任务 `29` 单次最终点击后平台仍无记录并保持 `need_action`。任何后续提交都必须先只读核对平台入库状态并取得项目所有者逐次授权，不能把旧任务自动重试。

## Git 状态

状态修复记录：抖音任务 `19/28` 与快手任务 `20/29` 的发布器超时结果已从长期 `processing` 纠正为 `need_action`；这些任务仍保持“不自动重试”，后续必须先人工核对并重新授权。Bilibili 任务 `27` 与 TikTok 任务 `33` 已依据精确标题和公开链接对账为 `success`。视频号任务 `34` 在未点击发表且发布列表无记录后结案为 `failed`；后续任务 `35` 已依据官方视频管理列表与空草稿箱的独立截图对账为 `success`，不伪造平台未提供的 Web 公链。

`main` 跟踪项目所有者仓库 `rainy321/geo-content-distribution-platform`，内容提效、P2 Web 接入及真实验收安全修复均推送到 `origin/main`；原 OmniPost 仓库仅作为 `upstream` 保留。
